{
  "name": "upi-assistant-local",
  "version": "0.1.0",
  "private": true,
  "description": "Static UPI Assistant App",
  "scripts": {
    "start": "npx serve public",
    "test": "echo \"Error: no test specified\" && exit 0"
  },
  "dependencies": {},
  "devDependencies": {
    "serve": "^14.2.3"
  }
}
